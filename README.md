# Generative-models-pytorch
Implementing generative models from scratch in PyTorch — Variational Autoencoder (VAE) and Denoising Diffusion Probabilistic Model (DDPM). Includes custom training loops, latent space visualization, and image generation.
